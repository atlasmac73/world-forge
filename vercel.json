{
  "framework": "nextjs",
  "buildCommand": "npm run check && npm test && npm run build",
  "installCommand": "npm ci",
  "crons": [
    {
      "path": "/api/heartbeat",
      "schedule": "0 9 * * *"
    }
  ]
}
